(()=>{
 const toast=(message,error=false)=>{document.querySelectorAll('.form-notice').forEach(el=>el.remove());const box=document.createElement('div');box.className='notice form-notice'+(error?' error-notice':'');box.setAttribute(error?'role':'aria-live',error?'alert':'polite');box.textContent=message;document.body.append(box);window.setTimeout(()=>{box.classList.add('notice-leaving');window.setTimeout(()=>box.remove(),300)},4200)};
 const existing=document.querySelector('.form-notice');if(existing)window.setTimeout(()=>{existing.classList.add('notice-leaving');window.setTimeout(()=>existing.remove(),300)},4200);
 const form=document.querySelector('.contact-form');if(!form)return;
 form.addEventListener('submit',async event=>{event.preventDefault();const button=form.querySelector('button[type="submit"],button:not([type])');if(button)button.disabled=true;try{const response=await fetch(form.action,{method:'POST',body:new FormData(form),headers:{Accept:'application/json','X-Requested-With':'XMLHttpRequest'}});const result=await response.json();if(!response.ok||!result.ok)throw new Error(result.message||'No fue posible enviar el mensaje.');toast(result.message||'Su mensaje ha sido enviado.');form.reset();if(history.replaceState)history.replaceState(null,'',location.pathname+location.hash)}catch(error){toast(error.message||'Revisá el resultado ingresado e intentá nuevamente.',true)}finally{if(button)button.disabled=false}});
})();
